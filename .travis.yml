language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - "pip install -r src/requirements.txt"
  - "pip install -r src/requirements_test.txt"
script:
  - "pytest -rootdir=$TRAVIS_BUILD_DIR/src/tests"
after_success:
  - "coveralls"
